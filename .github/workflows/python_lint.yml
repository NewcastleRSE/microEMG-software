name: Python Linton:  pull_request:    branches:      - dev      - mainjobs:  lint:    runs-on: ubuntu-latest    steps:      - uses: actions/checkout@v4      - uses: psf/black@stable        with:          options: "--check --verbose"          version: "23.12.1"        